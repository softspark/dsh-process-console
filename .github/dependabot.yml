version: 2
updates:
  # Keep GitHub Actions current (incl. any SHA-pinned third-party action).
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: chore
